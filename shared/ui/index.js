// @openframe/ui — Vue components shared between the device web UI (frontend/)
// and the fleet CMS (cms/), so both render from one component set (#45).
// Consumed as source via each app's `@shared` Vite alias.
export { default as Sparkline } from './Sparkline.vue'
