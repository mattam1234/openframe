# OpenFrame — Feature Backlog (100 items)

A working backlog of features worth integrating into OpenFrame, grouped by area.
Items already tracked as open/in-progress in
[`docs/fleet-and-mesh-roadmap.md`](docs/fleet-and-mesh-roadmap.md) are marked **(roadmap)**.

Legend: `- [ ]` not started · `- [~]` partial · `- [x]` done · **P1** high value / foundational · **P2** solid · **P3** nice-to-have / larger effort

---

## Firmware & Core Runtime

- [x] 1. **P1** Config schema versioning + auto-migration on boot — *survive old LittleFS JSON formats without bricking saved configs.*
- [x] 2. **P1** Atomic config writes (write-temp-then-rename) — *no more corrupt config on power loss mid-save.*
- [x] 3. **P2** Config backup/restore slots with one-tap rollback — *undo a bad config push instantly.* (auto-snapshot before every config apply + manual "back up now"; ring of 5 in /cfgbak; REST create/list/restore/delete (restore validates JSON, snapshots current first, reboots); Settings UI list with Restore/Delete)
- [x] 4. **P2** Factory-reset long-press that wipes config but keeps WiFi — *recover a misconfigured device in the field.* (ConfigManager.factoryResetKeepWifi; boot-time GPIO long-press via opt-in device.reset_pin/hold_ms; gated POST /api/factory-reset (snapshots first); Settings two-step danger button)
- [x] 5. **P1** Watchdog + safe-mode boot after repeated crash loops — *device stays reachable instead of dying silently.*
- [x] 6. **P2** Crash/panic backtrace stored to LittleFS, surfaced in UI — *diagnose resets without a serial cable.* (HealthMonitor logs panic/watchdog/brownout resets to /crashlog.json (rolling 20, crash-only to spare flash); GET /api/crashlog; Dashboard "Recent crash resets" panel. ⚑ full stack backtrace would need an ESP32 coredump partition — partition-table change deferred.)
- [ ] 7. **P3** Deep-sleep / light-sleep power profiles — *unlocks battery-powered nodes.*
- [x] 8. **P2** Brownout detection + state persist before reset — *don't lose state on flaky power.* (periodic dirty-flush of persistent variables every 30s so an unexpected reset loses ≤1 interval; brownout resets detected + logged via [[#6]] crashlog. Note: true persist-*during*-brownout isn't reliable in the ISR, so periodic flush is the robust approach.)
- [x] 9. **P3** Boot-time self-test (RAM, flash, I2C scan) with report — *catch bad hardware early.* (GET /api/selftest reports RAM (free/min/largest-block, ok if >20KB), flash (sketch/free space), filesystem (total/used/free), and an I²C bus scan (0x01–0x7E → found addresses); aggregate `ok`. Dashboard "Run self-test" button renders the report. ⚑ exposed on-demand rather than blocking boot with the I²C scan; reset reason already logged at boot via [[#6]].)
- [x] 10. **P1** Local cron/scheduler engine for time-based actions — *automations without cluster/NTP dependency.* (interval = no clock needed; daily = needs NTP)
- [x] 11. **P2** Typed variables (enums, ranges, units, persist flags) — *richer, validated automation data.* (Variable gains optional min/max range (clamps Int/Float sets), unit suffix, and enum options (rejects out-of-set String sets); (de)serialized in variables.json + exposed via /api/variables. persist flag already existed. Defined via variables.json; a dedicated variable-definition editor UI is a follow-up.)
- [x] 12. **P3** RTC (DS3231) support — *accurate time with no WiFi/NTP.* (DS3231 read/write over raw I²C (BCD ↔ epoch via Hinnant's days-from-civil, no lib); TimeManager seeds the system clock from the RTC at boot (new `rtc` source) and re-syncs the RTC from NTP once it lands — distinguished from an RTC-seeded clock via the SNTP sync-notification callback (ESP32/ESP8266 guarded). Config `time.rtc_enabled`/`rtc_address` + Settings toggle. ⚑ code-verified, not run against a physical DS3231.)

## Hardware Support

- [x] 13. **P2** Stepper driver support (A4988/DRV8825/TMC2209) — *motion projects.* (step/dir driver: STEP=`pin`, DIR=`pin_dir`, optional active-LOW `pin_enable`; non-blocking stepping toward an absolute target in loop() at up to `max_step_hz`; emits `PositionReached`. REST `move`+`position` and action-step `move` (own `position` field, round-trips); LayoutDesigner type + DIR-pin field; OutputControlCard jog/target control. ⚑ microstepping/accel ramps deferred.)
- [x] 14. **P1** Servo output type with angle/sweep actions — *common, high-demand actuator.*
- [x] 15. **P2** Encoder-with-button composite input — *menu navigation in one control.* (rotary encoder input type: polled quadrature A/B decode (transition table, 4 quarter-steps/detent) emits `RotateCW`/`RotateCCW`; optional debounced push-button emits `Press`/`Release`. Config via inputs.json `encoders[]` (pin_a/pin_b/pin_button/pullup), round-trips through /api/inputs; Action Manager gains the rotate events. ⚑ polled (fine at hand speed; very fast spins could skip); configured as JSON (no dedicated form UI yet); not run on physical hardware.)
- [x] 16. **P3** ESP32 native capacitive touch pins as input — *touch buttons with no extra hardware.* (digital inputs gain an opt-in `touch` flag + `touch_threshold`: when set, the pin is read via `touchRead()` (pressed below threshold) and flows through all the existing press/hold/multi-press/repeat logic. `#if ESP32` guarded (no-op on ESP8266); LayoutDesigner Touch checkbox. ⚑ code-verified on both build targets; not exercised on physical touch hardware.)
- [x] 17. **P2** More sensors: SCD40/41, SGP30, VL53L0X, MAX6675 — *air quality, ToF, thermocouple coverage.* (four new sensor drivers behind the existing `SensorDriver` registry: `sgp30` (eCO₂/TVOC), `vl53l0x` (ToF distance), `max6675` (K-type thermocouple, SPI: clock_pin/cs_pin/pin), `scd4x` (SCD40/41 CO₂/temp/humidity). Added vendor libs to platformio.ini; compile-verified on esp32dev + esp8266dev (esp8266 flash 88%). ⚑ code-verified against the libraries, not run on physical sensors.)
- [x] 18. **P3** Load cell + HX711 (weight/force) — *scales and force sensing.* (`hx711` sensor type: 2-wire bit-bang (no library) — `pin`=DT, `clock_pin`=SCK; reads the 24-bit two's-complement value (gain-128 channel A), reports `raw` + scaled `weight` = (raw − tare_offset)/scale; 200 ms ready-timeout guard. Fields round-trip via /api/sensors. ⚑ code-verified, not run against a real HX711.)
- [x] 19. **P1** Addressable-LED effect presets (rainbow, chase, breathe, fire) — *instant polish for WS2812 users.* (added Fire effect; rest already existed)
- [ ] 20. **P3** E-paper (Waveshare SPI) display driver — *low-power info panels.*
- [x] 21. **P3** Matrix keypad input driver — *keypad entry / PIN pads.* (row/col scanned keypad: rows driven low one at a time, columns read with pull-ups; per-key debounce emits `KeyPress`/`KeyRelease` with the row-major key label. Config via inputs.json `keypads[]` (rows/cols/keys), round-trips through /api/inputs; Action Manager gains the key events. ⚑ JSON-configured (no form UI); code-verified, not run on physical hardware.)
- [x] 22. **P2** Generic I2C/SPI register-map plugin (add chips via JSON) — *new sensors with no reflash.* (new `i2c_generic` sensor type: a `registers[]` map in sensors.json (metric, reg, bytes, big_endian, signed, scale, offset) read over raw Wire — add a new I²C chip by editing JSON, no driver/lib/reflash. Map round-trips through /api/sensors. ⚑ I²C only (SPI not covered); register map edited as JSON (no dedicated form UI); code-verified, not run against a physical chip.)
- [x] 23. **P3** PWM gamma-correction curves for LED/RGB — *visually linear dimming.* (opt-in per-output `gamma` flag; `pwmDuty()` maps the 8-bit level through γ≈2.8 (exact at 0/255) before scaling to PWM duty, used by LED brightness + RGB channels. Persisted in outputs.json + /api/outputs, surfaced in /api/outputs/state; LayoutDesigner γ checkbox for led/rgb.)

## Connectivity & Protocols

- [x] 24. **P1** mDNS so devices are `openframe-xxxx.local` — *find devices without hunting IPs.*
- [x] 25. **P2** Static IP / DHCP reservation hints in WiFi settings — *stable addressing for the CMS.* (optional `wifi.static_ip`/gateway/subnet/dns1/dns2; WiFi.config() applied before begin() in STA mode, with sane fallbacks (gw=x.x.x.1, dns=gw) and graceful fallback to DHCP if it won't parse/apply. Settings UI Static IP section. Blank IP = DHCP.)
- [x] 26. **P2** Multi-AP credentials with roaming to strongest network — *resilient connectivity.* (already delivered: `wifi.networks[]` credential list (Settings add/remove UI) + `startSTA` scans and connects to the **strongest configured** SSID on every connect/reconnect. ⚑ active mid-connection roam isn't done — impractical on the ESP WiFi stack; reconnect-to-strongest is the pragmatic equivalent.)
- [ ] 27. **P3** Ethernet (W5500/LAN8720) support — *wired, reliable nodes.*
- [x] 28. **P1** MQTT over TLS with CA cert upload — *secure broker links beyond the LAN.* (TLS client + CA at /mqtt_ca.pem or insecure mode; Settings UI)
- [x] 29. **P1** MQTT reconnect backoff + last-error in UI — *no silent disconnects.*
- [ ] 30. **P3** Matter/Thread bridge investigation — *native smart-home pairing.*
- [x] 31. **P2** Richer HA entity types (climate, cover, light brightness) — *fuller Home Assistant integration.* (RGB/WS2812 outputs and PWM-dimmable LEDs now publish as HA `light` entities (default schema) with brightness (scale 255) and, for colour outputs, RGB — via dedicated brightness/rgb command+state sub-topics wired to OutputManager; state echoed on change. Plain on/off outputs stay switches. ⚑ climate/cover not added — no matching hardware abstraction yet; light brightness/RGB was the high-value gap. ⚑ HA brightness drives ws2812 (FastLED master brightness) but is a no-op for analog 3-channel RGB outputs — scaling those in applyRgb is a regression trap since OutputState.brightness defaults to 0 (would black them out until a brightness cmd arrived); left safe by design.)
- [x] 32. **P1** Outbound webhook action (POST JSON, templated) — *integrate with anything (Slack, IFTTT, custom).* (HTTP action now interpolates `{{variable}}` in URL + body — see [[#40]])
- [~] 33. **P3** REST rate limiting + ETag/caching on config — *robustness under load.* (GET /api/config now sends an FNV-1a `ETag` + `Cache-Control: no-cache`; an `If-None-Match` match returns 304 so polling clients skip the payload. ⚑ rate-limiting deferred — the AsyncWebServer is already connection-limited (a natural throttle) and per-IP tracking there adds complexity for little gain.)
- [x] 34. **P2** NTP pool config + timezone/DST handling — *correct local time everywhere.* (new `time` config: two NTP servers + POSIX TZ string (applied via setenv/tzset). System clock stays UTC; daily schedules now resolve via localtime_r so "daily at HH:MM" fires at local wall-clock with automatic DST. Settings UI: NTP fields + a TZ preset combobox (CET/UK/US/AU/JP/IN…). ⚑ day-rollover key uses tm_year/tm_yday since tm_gmtoff is absent in this newlib.)

## Automation Engine

- [x] 35. **P1** Visual rule builder (trigger → condition → action) in the UI — *automations without hand-editing JSON.* (already provided by the Action Manager: trigger + conditions + ordered steps, all form-driven)
- [x] 36. **P1** Action steps: if/else branch + bounded loop — *real conditional logic.* (if/else/endif + repeat/endrepeat interpreter with bracket matching + guard; UI step types)
- [x] 37. **P2** wait-until-variable / wait-for-event with timeout — *event-driven sequencing.* (new `wait_until` action step: blocks (delay(10)-yield loop, watchdog-safe) until `variable op value` holds or `delay_ms` timeout — 0 = 30s safety cap; timeout fails the step and aborts the action. Consistent with the engine's existing blocking `delay` step. UI: condition + timeout fields.)
- [x] 38. **P2** Debounce / throttle / cooldown trigger modifiers — *stop runaway/chattering triggers.* (per-input-trigger `debounce_ms` (trailing-edge: fires once the input is quiet for the window, coalescing a burst — evaluated in loop()) and `cooldown_ms` (leading-edge throttle: drops events within the window after a fire). Runtime state per action, reset on re-register; (de)serialized in actions.json + REST; Action Manager fields shown for input triggers.)
- [x] 39. **P2** Scenes — snapshot + restore many outputs/vars at once — *one-tap "movie mode" style presets.* (new `SceneManager` (read-modify-write /scenes.json, ≤32): captures live output states (via OutputManager.fillStateJson) + all variable values; restore re-applies via OutputManager.applyStateJson + typed variable setters. REST GET/capture/restore/DELETE (child routes before parent per the route-matching gotcha); `scene_restore` action step (templated); Action Manager Scenes card (capture/restore/delete) + step picker.)
- [x] 40. **P1** Action templating with variable interpolation `{{node/x/temp}}` — *dynamic, reusable actions.*
- [x] 41. **P2** Per-action enable toggle + last-run time + pass/fail — *easier debugging & control.* (enabled flag was already enforced + a global pass/fail history existed; added an inline per-action last-run indicator (✓/✗ + time, derived from history) and a one-click enable/disable toggle in the action list (upsert-by-id, no editor round-trip).)
- [x] 42. **P3** Dry-run / simulate mode for actions — *test before it drives hardware.* (`ActionRunner::run(..., dryRun, log)` evaluates conditions + control flow but records a human-readable summary of each side-effecting step instead of executing it (no hardware driven, no vars mutated). `simulateAction` + POST /api/actions/simulate → {ok, steps[], error}; Action Manager flask button opens a "what would happen" dialog. ⚑ branches depending on in-action variable changes won't reflect them (state isn't mutated in a dry run).)
- [x] 43. **P2** Macro arguments/parameters — *one macro, many uses.* (macros gain `params[]` (variable_id → value) applied (type-aware) to VariableManager just before the macro's actions run, so one macro drives many outcomes via `{{variable}}` templating in its actions. (De)serialized in macros.json + /api/macros; Action Manager macro editor has an add/remove parameter list.)
- [x] 44. **P2** Cross-node automation ack/retry **(roadmap — open)** — *reliable distributed triggers.* (already delivered via [[#71]]: the `remote_action` step calls `NodeLinkManager::send(nodeId, Cmd, …)`, which is the **reliable unicast** path — per-message ack, retransmit up to 3×@300 ms, reboot-safe per-source seq dedup. ⚑ this guarantees *delivery*; end-to-end *execution-result* feedback to the originator is a separate follow-up.)

## Web UI

- [ ] 45. **P2** Vue port of the CMS dashboard **(roadmap)** — *one component set across device + fleet.*
- [x] 46. **P3** Dark/light theme toggle (persisted) — *user comfort.* (app-bar sun/moon toggle via Vuetify useTheme; persisted to localStorage, initial theme follows saved choice then OS `prefers-color-scheme` (dark default). Fixed a hardcoded `text-white` title that would vanish in light mode.)
- [x] 47. **P1** Live log console over WebSocket with filter/search — *debug without a serial cable.*
- [x] 48. **P2** Drag-and-drop dashboard widgets with saved layouts — *personalized monitoring.* (the Dashboard panels (Connection/Subsystems/Memory/Diagnostics) are reorderable by dragging their grip handle; order applied via CSS `order` on the flex row, persisted to localStorage, with a "Reset layout" button (shown only when customised) and graceful fallback for unknown/new panels. ⚑ reorders the main panels (not the KPI/Outputs rows); drag UX is code-verified, not exercised in a real browser.)
- [x] 49. **P1** Real-time sensor charts (in-browser ring buffer) — *see trends at a glance.*
- [x] 50. **P2** Mobile-responsive / PWA install — *use the UI as a phone app.* (web manifest (standalone, theme/bg colours, SVG icon) + apple-touch meta + a registered **network-passthrough** service worker — installable without caching stale `/www` chunks (see [[frontend-flashed-separately]]). Added the previously-missing favicon.svg. Vuetify layout is already responsive. ⚑ PNG maskable icons + build-hash-versioned offline cache are polish follow-ups.)
- [x] 51. **P3** i18n scaffolding for the frontend — *multi-language reach.* (vue-i18n wired into the app (`src/i18n.js`, Composition API + global `$t`); structured `en` source locale (nav + common keys), localStorage-persisted active locale, `SUPPORTED_LOCALES` registry, and the nav drawer converted to `$t('nav.*')` as the worked pattern. ⚑ only `en` shipped — add a `locales/<lang>.json` + register it to translate; most view strings are still literal (incremental).)
- [x] 52. **P1** Guided first-run setup wizard — *smooth out-of-box onboarding.* (4-step stepper: name → WiFi → MQTT → review; /setup route, AP-mode dashboard CTA)
- [x] 53. **P2** JSON schema validation + diff preview in the FS editor — *prevent broken config saves.* (JSON syntax validation + Format + an LCS line-diff toggle (add/del/ctx vs the on-disk file) already existed; added per-file **structural** validation (`frontend/src/lib/configSchemas.js`, +11 Vitest tests) keyed by filename and verified against the firmware loaders: root-must-be-object, the collection key each `*Manager` reads (`outputs`/`sensors`/`actions`/`macros`/`scenes`/`variables`/`displays`) must be an array of objects, inputs.json's optional `digital`/`analog`/`encoders`/`keypads` arrays, and page `widgets`. Definitively-broken shapes block the save; likely-mistake shapes (e.g. a missing required array) warn with a one-click "save anyway"; any edit re-validates. Deliberately conservative — only mirrors shapes the firmware rejects/ignores, so a valid-but-unusual config is never blocked; unknown files keep syntax-only checking. The editor caption now reads "structure checked" vs "JSON validated" accordingly.)
- [x] 54. **P3** Keyboard shortcuts + command palette — *power-user speed.* (global Ctrl/Cmd+K command palette (`CommandPalette.vue`, mounted in App) — fuzzy-filter + ↑↓/Enter to jump between all named routes; built from `router.getRoutes()` with friendly labels/icons.)
- [~] 55. **P2** Accessibility pass (ARIA, focus, contrast) — *usable by everyone.* (first pass: keyboard skip-to-content link, a `:focus-visible` outline for keyboard users, `<main>` landmark with focus target, and `aria-label`s on the icon-only action/delete buttons across the main views (Actions, Macros, Scenes, Layout Designer, Profiles, Screen/Sensor designers; Filesystem already had `title`s). ⚑ ongoing — a full audit (contrast tokens, all dialogs/forms, AT testing) is incremental.)

## CMS / Fleet Management

- [x] 56. **P1** SQLite persistence for historical telemetry **(roadmap — deferred)** — *retain history across CMS restarts.* (already durable via atomic-JSON `JsonStore` — history survives restart; literal SQLite swap intentionally deferred to avoid a native dep that breaks the Node-20 CI. ⚑ revisit if retention/query needs grow)
- [x] 57. **P2** Richer drill-in: screen listing + live preview **(roadmap — open)** — *see what a device is actually showing.* (new `get_screens` fleet command — `DisplayManager::fillScreensJson` reports each active display's geometry, active page + title, and every widget's *resolved* text (post-`maxChars` truncation, plus any notification overlay), bounded to 24 widgets to stay inside the 2048 B / 1024 B-TLS MQTT buffer (`truncated` flag if exceeded). CMS relays it via read-only `GET /api/devices/:id/screens` (viewer-allowed); the device drill-in gains a Screens panel that reconstructs each display on a `<canvas>` — redrawing widgets on the Adafruit-GFX 6×8 px grid the firmware uses — with manual + auto-refresh (paused while the tab is hidden). +1 integration test. ⚑ logical reconstruction, not a framebuffer capture — faithful for the text/value widget displays OpenFrame renders (a true pixel grab would need per-provider readback the OLED/TFT drivers don't expose).)
- [~] 58. **P1** User accounts + roles (viewer/operator/admin) — *safe multi-user operation.* (role-tiered tokens shipped: admin = full, `CMS_VIEWER_TOKEN` = read-only via method gating; +4 tests. ⚑ full username/password accounts deferred — a security-model decision)
- [x] 59. **P1** Audit log of all pushed commands — *accountability and forensics.* (durable AuditLog records role+action+target+result for single/bulk/template/firmware pushes; GET /api/audit; +3 tests)
- [x] 60. **P1** Staged/canary OTA with auto-halt on failure rate — *don't brick the whole fleet at once.* (deploy `canary` N first; rest proceed only if canary ack-rate ≥ `minAckRate`, else halts; UI canary field. ⚑ ack-based; flash-success/version-change gating is a telemetry follow-up)
- [ ] 61. **P1** Signed firmware + on-device verification **(roadmap — security)** — *closes the fleet-RCE risk.* ⚑ SKIPPED (needs human decision): requires a signing key-custody policy (where the private key lives, algorithm — e.g. ESP32 Secure Boot vs app-level Ed25519) and an embedded public key, plus on-hardware verification. A CMS-served hash would be integrity-only, not authenticity — security theater against the spoofed-host threat. Needs your call before implementing.
- [x] 62. **P2** Config diff viewer before `apply_config` — *know exactly what will change.* (device page "Preview changes" computes a leaf-level JSON diff (added/removed/changed, colour-coded) of the config about to be pushed vs the last config pushed to that device from this browser (localStorage baseline), then requires an explicit "Confirm push & reboot". All diff values HTML-escaped. ⚑ baseline is per-browser, not the device's live config — a CMS-proxied read of the device's /api/config would make it authoritative (follow-up).)
- [x] 63. **P2** Scheduled fleet jobs (nightly reboot, config reconcile) — *hands-off maintenance.* (CMS `JobStore` + `JobScheduler`: interval or daily-at-UTC-minute schedules fan a fleet command (by tag/ids/all) via the broker; pure `dueJobs`/`isDue` logic (+5 tests), daily de-dup, startup-arming so a restart doesn't catch up. REST GET/POST/DELETE /api/jobs + /api/jobs/:id/run (audited); Jobs page (create/run-now/edit/delete) + nav link on every page.)
- [x] 64. **P3** Per-device notes/metadata + searchable grid — *operational context.* (Device gains free-text `notes` (≤2000 chars); registry.setNotes + gated PUT /api/devices/:id/notes (+1 test). Device page has a Notes editor (doesn't clobber while typing); fleet grid gains a free-text search box matching name/id/board/ip/tags/notes with a "N hidden" count.)
- [x] 65. **P2** Grafana/Prometheus exporter for fleet metrics — *pro-grade dashboards & alerting.* (CMS `/metrics` (text/plain v0.0.4, open like /health): fleet totals (devices, online, broker link) + per-device gauges (online, free heap, RSSI, uptime, CPU load) labelled `device="<id>"`. Pairs with the on-device [[#84]] /metrics for per-node scraping.)
- [ ] 66. **P3** Multi-tenant / multi-site grouping — *manage several locations.*

## Mesh / Multi-node

- [ ] 67. **P2** painlessMesh backend behind `INodeLinkBackend` **(roadmap)** — *self-healing larger networks.*
- [ ] 68. **P2** Coordinator/gateway failover & election **(roadmap)** — *no single point of failure.*
- [ ] 69. **P3** Per-link RSSI/quality in topology map **(roadmap)** — *spot weak mesh links.*
- [ ] 70. **P3** Remote display mirroring / live preview of a leaf **(roadmap)** — *monitor headless leaves.*
- [x] 71. **P1** NodeLink ack + retry + sequence dedup — *reliable mesh messaging.* (reliable unicast: per-msg Ack, retransmit up to 3× @300ms, per-source seq dedup with reboot-safe exact-match; broadcasts unaffected)
- [ ] 72. **P2** Mesh-wide variable namespace browser in the UI — *discover what data exists.*
- [ ] 73. **P2** Auto-channel negotiation gateway↔leaves — *resolves the open WiFi/ESP-NOW channel question.*
- [ ] 74. **P3** Over-mesh OTA for infra-less leaves (relayed) — *update nodes with no WiFi.*

## Security & Auth

- [x] 75. **P1** Per-device API token / auth on REST + WS — *device endpoints aren't open by default.* (opt-in `device.api_token`: gates all REST writes, uploads, deletes, the secret-bearing /api/config read, AND the whole FS browser read surface (download/list/stat/selftest). Header-only Bearer (no query-string token — avoids log/Referer leaks), constant-time compare. Frontend sends the token when set, downloads via header-auth blob, Settings field mirrors it to the browser. Hardened per security review (closed a HIGH fs-download read-bypass + a MEDIUM token-in-URL/timing finding). WebSocket state-changing frames (`config_save`/`config_change`, `page_navigation`, `action_trigger`) are now gated too: since a browser can't set an Authorization header on the WS upgrade (and this fork doesn't expose the upgrade token in the connect event), the client proves the token via a first-message `{type:"auth"}` handshake — the device tracks authed client ids (cleared on disconnect) and rejects mutating frames from unauthed sockets, closing the path that bypassed the REST write-gate. Frontend WS store auto-sends the handshake when a token is set. ⚑ by design, read-only telemetry (status/sensors/logs GETs + the WS snapshot/broadcast stream) stays open — it carries no secrets, mirroring the open `/metrics`. ⚑ in the open-LAN default (no token) the WS still has no Origin/CSRF check — the connect event can't read it in this fork; the token handshake is the protection once auth is enabled.)
- [ ] 76. **P2** HTTPS on the device web server — *encrypted local UI/API.*
- [x] 77. **P2** CSRF + same-origin checks on state-changing endpoints — *block cross-site abuse.* (requireAuth now rejects (403) any request whose `Origin` header host ≠ the device host, before auth or side effects — covering every gated write/delete + the secret config read in one place, enforced even in the open-LAN default. Missing Origin (curl/HA/same-origin nav) is allowed, since CSRF is browser-only.)
- [ ] 78. **P1** Secrets in encrypted NVS, not plaintext LittleFS — *protect WiFi/MQTT credentials.* ⚑ SKIPPED (needs hardware decision): real NVS encryption requires ESP32 **flash encryption** (irreversible eFuses — see #79) and ESP8266 has no encrypted key store, so plain-NVS would still be plaintext-on-flash. The *web* exposure (FS browser reading /config.json) is already mitigated by the #75 auth gating; the residual physical-flash-dump risk needs the flash-encryption fuse decision. Your call.
- [ ] 79. **P3** ESP32 flash encryption + secure boot build profile + docs — *hardware-rooted trust.*
- [x] 80. **P2** Rate-limit/lockout on captive portal + login — *resist brute force.* (CMS login already rate-limited (10/min/IP); added `FailureLockout` — 5 consecutive failures locks an IP for 5 min (resets on success), returning 429. +2 tests. ⚑ device-side captive-portal login has no token form yet (the device uses the #75 API-token model), so this covers the CMS login surface.)
- [x] 81. **P2** CMS HTTPS-behind-proxy + broker auth reference **(roadmap follow-up)** — *safe wider exposure.* (cms/DEPLOYMENT.md: dashboard admin/viewer tokens, TLS via Caddy/nginx reverse proxy (with WS upgrade headers + bind-to-localhost + CMS_PUBLIC_URL), mosquitto password_file + TLS listener, device-side broker creds, and a public-exposure checklist; linked from the CMS README.)
- [x] 82. **P1** CVE/dependency scanning in CI (frontend + CMS) — *catch vulnerable packages early.* (Dependabot + non-blocking `npm audit` job)

## Observability & Diagnostics

- [x] 83. **P2** Structured remote logging to CMS/syslog — *centralized fleet logs.* (a Logger listener mirrors Warning+ entries to `<baseTopic>/<deviceId>/log` as JSON {ts, level, tag, msg} over MQTT, re-entrancy-guarded so a publish that logs can't recurse, only when connected. ⚑ Warning+ only (avoids flooding); CMS-side ingestion/display UI is a follow-up — the device publishes to the standard fleet topic any subscriber/syslog bridge can consume.)
- [x] 84. **P2** On-device `/metrics` in Prometheus format — *standard scraping.* (GET /metrics, text/plain v0.0.4: uptime, free/min/largest heap, heap-fragmentation %, CPU load, boot count, safe-mode, wifi up/RSSI, mqtt up — each labelled `device="<id>"`. Left open like other read-only status endpoints so scrapers need no token.)
- [x] 85. **P1** Enhanced "identify" — blink LED + flash screen + buzzer — *physically locate a device fast.*
- [x] 86. **P3** Network diagnostics page (ping, MQTT latency, DNS, NTP) — *troubleshoot connectivity in-UI.* (GET /api/netdiag: WiFi (ssid/rssi/ip/gateway/subnet/dns), timed DNS resolution of the NTP host (connectivity probe), broker TCP-connect reachability + latency, and NTP source/validity. Dashboard "Network diagnostics" button renders it. ⚑ uses DNS-resolve + TCP-connect timing rather than ICMP ping (not available in the Arduino stack).)
- [x] 87. **P2** Heap fragmentation tracking + leak warning — *catch slow memory leaks.* (HealthMonitor samples free heap + largest contiguous block once per 5s window → session min-free watermark + fragmentation % (`of_largest_free_block` added for both ESP32/ESP8266); logs a single LOG_W when free<20KB or frag≥60%, re-arming on recovery. Surfaced in /api/status (largestFreeBlock, heapFragPercent) and /metrics [[#84]].)
- [x] 88. **P3** Event-bus inspector (live events) — *debug automations visually.* (the WS already broadcasts every EventBus event; new Event Inspector view (`/events`, nav + palette) renders the live stream with type/source/payload, free-text + type filters, pause (freezes snapshot) and clear (reference-boundary, doesn't mutate the shared buffer).)

## Developer Experience / CI / Testing

- [x] 89. **P1** CI: build all 3 ESP targets + frontend + CMS tests on every PR — *no more "works on my machine".*
- [ ] 90. **P1** Native/host unit tests (Action engine, config merge) via PlatformIO native env — *fast logic tests, no board.* ⚑ BLOCKED (toolchain): no host C++ compiler (g++) in this build environment, so a `platform=native` test env can't compile/verify here. Deferred to avoid adding an unverifiable env that could destabilize the real ESP builds. (Note: the CMS already has a 56-test `node:test` host suite — see #89.) Needs a host with build-essential/g++ to implement+verify.
- [x] 91. **P2** Frontend unit/component (Vitest) + E2E (Playwright) tests — *UI regressions caught automatically.* (Vitest (`npm run test:unit`, in CI before build): 28 unit/component tests — `pinItems` board-pins, a `Sparkline` component mount, plus the `configSchemas` [[#53]] and `screenEditor` geometry helpers. **Playwright E2E now wired in** (`npm run test:e2e`): a `playwright.config.js` boots the Vite dev server itself and drives real Chromium; the device REST API is stubbed per-test via `page.route` on `/api/*` (so no hardware/broker needed — `e2e/helpers.js`). 8 specs covering the Layout Designer (card load, add-via-ghost-tile, tab switch, save-enabled) and the Screen Designer (widget render, **drag-from-library-onto-panel**, select→inspector, empty state). A dedicated CI `e2e` job installs Chromium (browser cache) and runs them, uploading the report on failure. ⚑ gotcha baked into the helper: a `**/api/**` glob also matches the app's own `/src/api/client.js` module — match on the exact `/api/` pathname prefix instead.)
- [x] 92. **P2** Pre-commit hooks: clang-format + eslint/prettier — *consistent style everywhere.* (in-repo `.githooks/pre-commit` lints staged frontend (`npm run lint`), typechecks staged cms (`npm run typecheck`), and clang-formats staged firmware *if* `.clang-format`+binary exist; skips areas with no node_modules. Enable with `git config core.hooksPath .githooks`; bypass with `--no-verify`. +README. Also added the missing **frontend** `.eslintrc.cjs` (Vue3-essential; the `npm run lint` script had no config and silently errored — fixed a real irregular-whitespace bug surfaced along the way) **and a new CMS ESLint setup** (@typescript-eslint, `npm run lint` — caught/fixed an unnecessary-regex-escape); both lint in CI and in this hook. ⚑ no `.clang-format` in repo yet, so the C++ step is dormant until one is added.)
- [~] 93. **P1** Device simulator/emulator — *develop UI + CMS without hardware.* (CMS fleet simulator `cms/tools/simulate.ts` via `npm run simulate` — N fake devices publish presence + drifting heartbeats over MQTT and ack commands, so the dashboard/topology/history/alerts/bulk UI work with no hardware. ⚑ a mock device-API server for the Vue app's REST/WS is a follow-up.)
- [x] 94. **P2** Automated LittleFS image build + size-budget check in CI — *catch oversized assets before flash.* (`firmware/tools/check-fs-size.mjs` sums firmware/data, prints the 10 largest files + % of budget, fails over BUDGET_BYTES (default 1.5 MiB, `--budget=`/`FS_BUDGET_BYTES` override). CI runs it after the frontend build. Current UI ≈ 1.0 MiB (66.5%). ⚑ tune the budget down to the smallest UI-bearing target's LittleFS partition.)
- [x] 95. **P2** Release automation: tag → artifacts → GitHub release (read by update check) — *one-click releases.* (`.github/workflows/release.yml` on `v*` tags: builds the UI (+FS size check), then per-target firmware.bin and LittleFS image for all 5 envs, and publishes a GitHub Release (auto notes) with `openframe-<env>-firmware.bin`/`-littlefs.bin` assets — which the device's OTA update-check reads.)
- [ ] 96. **P3** Hardware-in-the-loop smoke test per release — *verify on real silicon.*

## Documentation & Community

- [x] 97. **P2** Hardware wiring diagrams + supported-parts matrix — *lower the barrier to building.* (docs/hardware-guide.md: boards table, shared bus/power/level-shifting notes, and supported-parts matrices for every output/input/sensor type (incl. the new servo/stepper/touch/encoder/keypad/HX711/i2c_generic) with wiring + pin-selection rules and ASCII wiring snippets; linked from README.)
- [x] 98. **P1** End-to-end "your first automation" tutorial — *fast time-to-value for new users.* (docs/first-automation-tutorial.md: power-on → wizard → define LED+button → button-toggles-LED action + notification → schedule/other triggers → troubleshooting; linked from README. Uses real shipped features.)
- [x] 99. **P2** Plugin authoring guide with a worked example — *grow the plugin ecosystem.* (docs/plugin-authoring-guide.md: the no-reflash path (add an I²C sensor via `i2c_generic` [[#22]] with a full register-map worked example + field table), a worked "reading light" macro/parameters/scene example, and when to graduate to the C++ registries; linked from README.)
- [x] 100. **P1** Choose and add a LICENSE (currently TBD) — *unblocks adoption & contribution.* (added **MIT** LICENSE — the standard permissive default for firmware/IoT, maximising the adoption this item targets — and linked it from the README. ⚑ default choice (copyright "OpenFrame contributors"); swap to Apache-2.0/GPL and update the holder if you prefer.)

---

_Generated as a planning backlog — re-prioritize freely. Update checkboxes as items land._
